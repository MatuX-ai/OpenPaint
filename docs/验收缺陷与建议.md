# OpenPaint 验收缺陷与建议登记

> 本文件汇总本次审计验收发现的问题、风险与改进建议，按"阻塞 / 重大 / 中 / 低 / 建议"分级。
> 每条缺陷含编号、模块、复现 / 证据、建议处理方式、关联测试或文档。
> **最后更新**：2026-08-27

---

## 0. 验收结论摘要

| 维度 | 结论 |
| --- | --- |
| 后端 Rust 单测 | 38/38 通过 ✅（含 W8 新增 6 个 image 转码测试） |
| 前端 Vitest 单测 | 178/178 通过 ✅（含 W7/W8 新增 95 个 composable + 组件测试） |
| 前端类型检查 | 0 错误 ✅ |
| 前端 ESLint | 0 错误（211 警告，风格类）✅ |
| 一键审计脚本 | 5 阶段全部 PASS（约 41s）✅ |
| 阻塞缺陷 | **0** |
| 重大缺陷 | **0** |
| 中等缺陷 | 1（R-A02 — Hermes 分发；R-A01 组件测试 ✅ 已修复） |
| 低风险 / 改进 | 5（R-A03 ~ R-A07） |
| UX 类（已纳入 UX 需求文档） | 11（UX-A01 ~ UX-A11，其中 9 项 ✅ 已修复，剩 UX-A07 / A08 / A09 待 W9+） |

> 总体：阶段一（MVP 核心闭环）可验收通过；阶段二（AI 助理）链路打通；阶段三（智能增强）按里程碑计划保留为后续工作。

---

## 1. 缺陷 / 风险登记

| ID | 等级 | 模块 | 描述 | 证据 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| **R-A01** | 中 | 前端测试 | 缺组件级单元测试（Vue Test Utils），目前仅覆盖 Pinia store 与工具函数 | `src-web/src/components/**/*.test.ts` 0 文件 | 已修复 ✅ W8：引入 `@vue/test-utils` + happy-dom，新增 11 个组件测试文件（AppButton/AppModal/ToastContainer/MenuDropdown/AppMenuBar/OnboardingCard/NewCanvasDialog/ExportDialog/BatchExportDialog/UnsavedConfirmDialog/KeyboardCheatsheet）共 56 个组件用例 | 已修复 |
| **R-A02** | 中 | Agent / 部署 | Hermes Agent 二进制依赖未在仓库内随包分发；首次启动需手动放置 | `src-tauri/bin/hermes` 占位；`agent/manager.rs::find_hermes_binary` 测试显示无二进制不 panic | 文档化 `src-tauri/bin/hermes` 安装步骤；README 中给出官方下载链接；或在 CI 中下载产物 | 待办 |
| **R-A03** | 低 | 图库 | 缺少云端备份策略（仅本地 SQLite） | `gallery/database.rs` 实现 | 提供可选 S3 / WebDAV 备份插件；以"用户自配存储"方式落地 | 计划 |
| **R-A04** | 低 | 测试 / E2E | 未做端到端自动化（Playwright / WebDriver） | `package.json` 无 `test:e2e` | 已规划 · W9+；详见 [docs/e2e-test-plan.md](./e2e-test-plan.md)（5 个最小场景 + Playwright 推荐 + 数据隔离 + 占位脚本）；先覆盖"打开→新建画布→落图→AI 对话"主路径 | 计划 |
| **R-A05** | 低 | 依赖管理 | `Cargo.lock` 已生成但未配置自动升级 | `Cargo.lock` 存在；无 Renovate / Dependabot | 引入 Renovate / Dependabot；为 `resvg / usvg / image` 等图形栈启用 minor 自动 PR | 计划 |
| **R-A06** | 低 | 发布 | Windows 代码签名仅本地测试 | `openpaint-codesign.pfx` 占位；`CODE_SIGNING_SETUP.md` 描述 EV 流程 | CI 中 `osslsigncode` 已就绪；验证 EV 证书替换流程；将签名密钥托管至 GitHub OIDC | 待办 |
| **R-A07** | 低 | ESLint 警告 | 211 条 Vue 风格警告（max-attributes-per-line / html-self-closing / singleline-html-element-newline 等） | `.audit-logs/eslint-*.log` | 运行 `pnpm lint:fix` 自动修复 169 条；剩余 42 条需人工调整 | 计划 |
| **UX-A01** | 重大 | 前端 UX | TopBar 缺文件 IO 入口（新建/打开/保存），无菜单栏，无标题栏未保存指示 | `src-web/src/components/layout/TopBar.vue:50-101`、`MainLayout.vue` | 已修复 ✅ W7；AppMenuBar + 💾 + 标题栏 + Toast 全套 | 已修复 |
| **UX-A02** | 重大 | 前端 UX | 画布关闭后无"未保存"提示；任何笔触丢失风险 | `MainLayout.vue` 未注册 `onCloseRequested` | 已修复 ✅ W8；`AppView.installCloseGuard` 拦截 `onCloseRequested` + UnsavedConfirmDialog | 已修复 |
| **UX-A03** | 中 | 前端 UX | 快捷键缺失 Ctrl+N / O / S / Shift+S / E / Shift+E / A / D / C / V / + / - / 0 / F11 / ? | `src-web/src/composables/useShortcuts.ts:95` | 已修复 ✅ W7；`useShortcuts.defaultBindings()` 补齐 14 个快捷键 | 已修复 |
| **UX-A04** | 中 | 前端 UX | `Ctrl+S` 没接 `galleryApi.save`，UI 无 Save 按钮 | `src-web/src/api/index.ts:208` 已具备 `save`，但 `TopBar.vue` 未引用 | 已修复 ✅ W7；TopBar 加 💾 + `Ctrl+S` + 菜单"保存到图库" | 已修复 |
| **UX-A05** | 中 | 前端 UX | 主画布不支持拖拽 PNG/JPG/WebP/SVG | `src-web/src/components/canvas/CanvasView.vue` 无 drop 监听 | 已修复 ✅ W8；CanvasView 加 `dragover/drop` + `useFileActions.importFromFiles` | 已修复 |
| **UX-A06** | 中 | 前端 UX | README 主打的"一键多尺寸导出"无 UI | `src-web/src/components/canvas/` 无 ExportDialog | 已修复 ✅ W8；ExportDialog + BatchExportDialog + Rust `render_canvas_image` | 已修复 |
| **UX-A07** | 中 | 前端 UX | AI 助理未配置时浮窗空状态无 CTA | `src-web/src/components/assistant/AIAssistant.vue` | 待办（W8+ 阶段） | 待办 |
| **UX-A08** | 低 | 前端 UX | 左工具栏仅 tooltip，无快捷键字符 chip | `src-web/src/components/layout/LeftSidebar.vue:34-45` | 待办 | 待办 |
| **UX-A09** | 低 | 前端 UX | 主工具条 `CanvasToolbar.vue` 信息密度太低，非画笔工具下几乎为空 | `src-web/src/components/canvas/CanvasToolbar.vue` | 待办 | 待办 |
| **UX-A12** | 重大 | 资产库 | 无内置图标库 / 画刷库 / 调色板，用户首启动“开箱不可玩” | 整个项目 | 已规划 · 详见 [`docs/asset-library-requirements.md`](./asset-library-requirements.md) v0.1.0（W9/W10/W11 落地）。采用 Iconify + 8 PNG 画刷 + 调色板 / 渐变预设策略，安装包增量 ≤ 3 MB | 计划 |
| **UX-A13** | 中 | MCP / AI | MCP 工具集仅 10 个，AI 无法处理图标 / 渐变 / 调色板请求 | `src-tauri/src/agent/mcp.rs::tool_definitions()` | 已规划 · 同上。计划扩到 14 个工具 | 计划 |
| **UX-A10** | 低 | 前端 UX | 快捷键速查面板缺失 | 整个项目无 KeyboardCheatsheet | 已修复 ✅ W7；`KeyboardCheatsheet.vue` + `?` 触发 | 已修复 |
| **UX-A11** | 低 | 前端 UX | 按钮缺 aria-label / focus-visible 样式 | `TopBar.vue` / `LeftSidebar.vue` 等 | 已修复 ✅ W7/W8；TopBar / OnboardingCard / AppMenuBar 已加 aria-label；AppModal 加 `:focus-visible` 全局样式 | 已修复 |

---

## 2. 已修复（本次审计过程）

| ID | 模块 | 描述 | 修复方式 | 状态 |
| --- | --- | --- | --- | --- |
| **R-F01** | 审计脚本 | `Start-Process -FilePath 'pnpm'` 在 PS 5.1 下报错 "%1 is not a valid Win32 application" | 将 pnpm 调用统一改为 `cmd.exe /d /s /c pnpm <args>`，借助 cmd.exe 解析 corepack shim；同时抽出 `Invoke-AuditStep` 公共函数消除重复 | 已修复 ✅ |
| **R-F02** | 审计脚本 | 原脚本包含中文字符，PS 5.1 在无 BOM UTF-8 输入下解析失败 | 重写为 ASCII-only 文案；控制台 / 文件输出仍可保留 UTF-8 编码 | 已修复 ✅ |
| **R-F03** | 文档 | 测试用例编号不统一 | 在 `docs/测试用例集.md` 中统一为 `TC-{模块缩写}-{三位序号}` 规范 | 已修复 ✅ |

---

## 3. 阶段二 / 阶段三未实现项（设计文档已声明）

| ID | 关联文档 | 描述 | 当前状态 | 备注 |
| --- | --- | --- | --- | --- |
| **R-N01** | 《项目说明书》§6.3 | LanceDB 向量搜索 | 🟡 预留 | `Cargo.toml` `vector-search` feature flag + 可选 `lancedb` 依赖已写好；UI 与 API 暂未启用 |
| **R-N02** | 《项目说明书》§6.3 | 图库语义搜索 | ❌ 未实现 | 当前仅 `LIKE` 文本搜索（`TC-GAL-003 / TC-GAL-004`） |
| **R-N03** | 《项目说明书》§6.3 | AI 自动标签建议 | ❌ 未实现 | 当前依赖用户 / Agent 显式打标签 |
| **R-N04** | 《项目说明书》§6.3 | 插件系统 UI（注册面板） | 🟡 部分 | MCP 协议已支持外部工具（10 个内置 + `placeholder` 扩展点）；UI 注册面板待开发 |
| **R-N05** | 《项目说明书》§6.3 | 4K 画布 60fps 性能优化 | 🟡 待基线 | 当前 MVP Canvas 2D；Skia / WebGPU 升级为后续里程碑 |
| **R-N06** | 《项目说明书》§6.3 | 跨平台打包本地全平台验证 | 🟡 CI 通过 | `pnpm tauri build` 在 CI 中跑通；本地 macOS / Linux 端到端验证尚未完成 |

---

## 4. 安全 / 隐私关注（建议评估）

| ID | 维度 | 描述 | 建议 |
| --- | --- | --- | --- |
| **R-S01** | LLM API Key | 当前 key 通过 `default_config.yaml` 字段读取 | 引入系统 keychain（Tauri `tauri-plugin-stronghold` 或 OS 凭据）；明文 YAML 仅作占位 |
| **R-S02** | 图库文件 | SQLite + 缩略图全部存放 `~/.openpaint`，未加密 | 文档化备份建议；后续可加本地加密（`sqlite-rs` + AES-GCM） |
| **R-S03** | 截图上传 | "截图→AI"链路可能含敏感内容 | UI 上明确"发送前预览"；AI 引擎设置中支持"本地 LLM 优先"（Ollama） |
| **R-S04** | MCP 子进程 | `bin/mcp.rs` 通过 stdio 与父进程通信 | 当前为受信 Rust 二进制；如未来允许第三方 MCP，需做签名校验 |
| **R-S05** | 代码签名 | macOS / Windows 端缺 EV 证书 | 与 R-A06 一并解决 |

---

## 5. 文档改进建议

| ID | 文档 | 建议 |
| --- | --- | --- |
| **R-D01** | `DEVELOPMENT.md` | 新增"Windows 上首次构建（含 Hermes 二进制下载）"小节，呼应 R-A02 |
| **R-D02** | `RELEASING.md` | 补充 EV 代码签名替换流程的 step-by-step 命令 |
| **R-D03** | `CONTRIBUTING.md` | 显式说明测试编写规范：Rust 用 `#[cfg(test)] mod tests`；前端用 `*.test.ts`（Vitest 自动发现） |
| **R-D04** | `README.md` | 增加"功能矩阵"表格，区分"已实现 / 计划中 / 实验性" |
| **R-D05** | `OpenPaint 前端设计说明书.md` | 补 §13"组件测试规范"，与 R-A01 联动 |
| **R-D06** | `docs/kanban.md` | 把 R-A01 / R-A02 显式写入 W7 backlog |
| **R-D07** | `docs/ux-onboarding-requirements.md`（新增） | 新增独立 PRD，把 UX-A01~UX-A11 落到 US-1~US-11 + 验收标准 + W7/W8 backlog + ONB-xxx 测试矩阵 |

---

## 6. 流程改进建议

| ID | 流程 | 建议 |
| --- | --- | --- |
| **R-P01** | PR 检查 | 在 `.github/workflows/` 增加 `audit.yml`，在 PR 上自动跑 `run-audit-tests.ps1`，5 阶段全过才允许 merge |
| **R-P02** | 回归频率 | 每周一次全量审计；产物归档至 `.audit-logs/<date>/`，保留 90 天 |
| **R-P03** | 测试数据 | `gallery` 测试用临时 SQLite 文件，建议加入自动清理脚本（已在 `database.rs` 中实现 `rotate`，可复用） |
| **R-P04** | 审计脚本入口 | 在仓库根 `package.json` 增加 `"audit": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-audit-tests.ps1"`，方便跨平台统一调用 |
| **R-P05** | 文档同步 | 任何里程碑变更必须同步更新 `OpenPaint 项目说明书.md` §6 阶段表 + `docs/kanban.md` |

---

## 7. 已记录但暂不处理的"建议"

> 这些项不在验收范围内，但记录以备后续讨论。

| ID | 维度 | 描述 |
| --- | --- | --- |
| R-T01 | 性能 | 暂无 4K / 大量图层压测基线 |
| R-T02 | 国际化 | 前端硬编码中文文案，无 i18n 框架 |
| R-T03 | 主题 | 深色为默认，浅色主题部分组件（如 `Toolbar`）未完整适配 |
| R-T04 | 可访问性 | 缺 ARIA 属性 / 键盘焦点环显式样式 |
| R-T05 | 日志 | 前端缺统一日志门面（`console.*` 直散） |
| R-T06 | 监控 | 无前端错误上报（Sentry / 自建） |
| R-T07 | 资产库 | 无内置画刷 / 图标 / 调色板，“首启不可玩”是 OpenPaint 对标 paint.net / Photoshop 唯一的重大体验短板。已升级为 [`docs/asset-library-requirements.md`](./asset-library-requirements.md) 需求文档（v0.2.0 里程碑） |

---

## 8. 处理优先级总览

```
[阻塞]    ──── (无)
[重大]    ──── (无)
[中]      R-A02 (Hermes 分发)   W6 / W7
[低]      R-A03 ~ R-A07        后续里程碑
[已修复]  R-F01 / R-F02 / R-F03  本次审计
[已修复]  R-A01 (组件测试)      W7 / W8
[已修复]  UX-A01 / A02 / A03 / A04 / A05 / A06 / A10 / A11  W7 / W8
[未实现]  UX-A07 / A08 / A09    按 ONB 文档
[未实现]  R-N01 ~ R-N06        按设计文档 §6.3
```

---

## 9. 签字

| 角色 | 签字 | 日期 | 备注 |
| --- | --- | --- | --- |
| 审计组 | _____ | 2026-08-27 | 0 阻塞 / 0 重大 |
| 项目 Owner | _____ | 2026-08-27 | 同意进入下一里程碑 |
| 安全 Owner | _____ | 2026-08-27 | §4 安全项已确认范围 |

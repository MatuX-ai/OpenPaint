基于我们所有的讨论，我为你整理了一份完整的 **《OpenPaint 项目说明书》**。这份文档可以直接作为项目的 README 或技术白皮书使用。

---

# OpenPaint 项目说明书

**版本**：v1.0.0 | **状态**：规划中 | **开源协议**：MIT

---

## 1. 项目概述

### 1.1 项目名称

**OpenPaint** —— 开源 AI 原生设计工作台

### 1.2 一句话定位

> 一个以中央画布为核心、AI 为副驾驶的轻量级设计工具，让设计师通过自然语言驱动像素级编辑与矢量生成的无缝工作流。

### 1.3 项目愿景

让设计师专注于创意本身，而非在工具间反复切换。OpenPaint 不是另一个 Photoshop 克隆，而是一个**“AI 时代的画布”**——将像素编辑的精确性与 AI 生成的创造力融为一体，同时保持开源、轻量、可插拔的个性。

### 1.4 对标参考

- **交互理念**：DeepSeek Harness（用户自主接入大模型）
- **画布体验**：Paint.NET / PaintFE（轻量像素编辑）
- **AI 生成层**：Figma + AI 插件（矢量设计与生成）
- **智能体架构**：Hermes Agent（自主决策与工具调度）

---

## 2. 目标用户

| 用户类型       | 典型场景              | 核心诉求               |
| :------------- | :-------------------- | :--------------------- |
| **独立设计师** | UI/UX 设计、Logo 制作 | 效率提升，减少重复劳动 |
| **产品经理**   | 快速原型、需求验证    | 用自然语言生成界面素材 |
| **前端开发者** | 切图、图标导出        | 一键生成多尺寸资产     |
| **开源爱好者** | 学习、二次开发        | 模块化架构，易于扩展   |
| **AI 尝鲜者**  | 探索 AI 辅助设计      | 自选大模型，本地优先   |

---

## 3. 核心架构

### 3.1 技术栈总览

| 层级            | 技术选型                     | 职责                                  |
| :-------------- | :--------------------------- | :------------------------------------ |
| **桌面框架**    | Tauri v2 (Rust + WebView)    | 跨平台打包、高性能后端、系统调用      |
| **前端框架**    | Vue 3 + TypeScript           | UI 渲染、交互逻辑、状态管理           |
| **画布引擎**    | Canvas 2D / Skia (Rust)      | 图层系统、像素编辑、历史记录          |
| **AI 智能体**   | Hermes Agent (Rust 嵌入版)   | 意图理解、工具调度、自主决策          |
| **AI 生成引擎** | OpenPencil (Web 版)          | 矢量编辑、AI 图像生成（用户自配 Key） |
| **本地数据库**  | SQLite (rusqlite)            | 图库元数据、历史记录、标签索引        |
| **向量搜索**    | LanceDB（渐进式）            | 语义搜图、智能召回                    |
| **通信协议**    | MCP (Model Context Protocol) | 模块间解耦、工具注册与调用            |

### 3.2 架构图（概念）

```
┌─────────────────────────────────────────────────────────────┐
│                     OpenPaint 主窗口                        │
├───────────────┬─────────────────────┬─────────────────────┤
│   中央画布    │    AI 助理浮窗      │   OpenPencil 右窗   │
│  (像素工坊)   │   (右下角对话)      │  (AI 微调工坊)      │
│  · 图层系统   │   · 自然语言交互    │  · 矢量编辑         │
│  · 选区工具   │   · 自主决策        │  · AI 重绘         │
│  · 历史记录   │   · 图源抓取        │  · 导出落回        │
│               │   · 结果预览        │                     │
└───────┬───────┴──────────┬──────────┴──────────┬──────────┘
        │                  │                      │
        └──────────────┬───┴──────────────────────┘
                       │
              ┌────────┴────────┐
              │  Hermes Agent   │  ← 用户自配大模型 API
              │  (MCP 总线)     │     (OpenAI / Claude / 本地)
              └────────┬────────┘
                       │
              ┌────────┴────────┐
              │   图库管理系统   │
              │  SQLite + 文件   │
              │  向量搜索(未来)  │
              └─────────────────┘
```

---

## 4. 核心功能模块

### 4.1 中央画布（像素工坊）

| 功能     | 说明                                                               |
| :------- | :----------------------------------------------------------------- |
| 图层系统 | 支持无限图层、蒙版、混合模式、图层锁定                             |
| 基础工具 | 画笔、橡皮擦、选区（矩形/套索/魔棒）、裁剪、变形（缩放/旋转/翻转） |
| 历史记录 | 完整 Undo/Redo，支持历史快照                                       |
| 格式支持 | 导入：PNG, JPG, WebP, SVG；导出：PNG, JPG, WebP, PSD（兼容子集）   |
| 性能优化 | GPU 加速渲染，支持 4K+ 画布流畅操作                                |

### 4.2 AI 助理浮窗（右下角）

| 功能       | 说明                                                         |
| :--------- | :----------------------------------------------------------- |
| 对话交互   | 常驻浮动窗口，支持自然语言多轮对话                           |
| 自主决策   | AI 判断用户意图，决定是否调用 OpenPencil 或其他工具          |
| 图源抓取   | 自动截取当前画布选区/图层，转化为 Base64 传给 AI 引擎        |
| 结果预览   | 以居中弹窗（Modal）展示 AI 生成初稿，用户确认或拒绝          |
| 历史上下文 | 记忆前序对话，支持“修改上一版”等引用指令                     |
| 大模型自由 | 用户自配 API Key（OpenAI / Claude / DeepSeek / 本地 Ollama） |

### 4.3 OpenPencil 右窗（AI 微调工坊）

| 功能         | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| WebView 嵌入 | 在右侧面板加载 OpenPencil Web 版，无需切换软件               |
| 矢量编辑     | 完整的矢量图形编辑能力（节点、曲线、布尔运算）               |
| AI 重绘      | 利用 OpenPencil 的 AI 能力对传入图源进行风格转换、扩图、重绘 |
| 微调交互     | 用户可在右窗手动调整 AI 生成结果的细节                       |
| 落地机制     | 点击“OK”将最终结果覆盖到中央画布当前图层，自动归档图库       |

### 4.4 图库管理系统（历史资产库）

| 功能     | 说明                                                  |
| :------- | :---------------------------------------------------- |
| 自动归档 | 每次 AI 生成结果自动保存（含提示词、模型、时间戳）    |
| 缩略图   | 自动生成 WebP 缩略图，节省磁盘空间                    |
| 标签系统 | 用户可手动打标签，AI 也可自动建议标签                 |
| 分组管理 | 支持 Group ID，一键召回整套资产（如全套 Logo 尺寸）   |
| 语义搜索 | 渐进式集成 CLIP + LanceDB，支持自然语言搜图           |
| 存储策略 | 限制最大数量（默认 500 张），自动轮转，用户可手动归档 |

### 4.5 原子工具系统（Hermes Agent 驱动）

OpenPaint 不硬编码业务场景，而是提供 **10 个原子工具**，由 Hermes Agent 自主编排完成复杂任务。

| 分类         | 工具名称               | 职责                                            |
| :----------- | :--------------------- | :---------------------------------------------- |
| **画布交互** | `get_canvas_selection` | 获取当前选区/图层为 Base64                      |
|              | `get_selection_bounds` | 获取选区坐标与尺寸                              |
|              | `paste_image_to_layer` | 将图片贴入画布当前图层                          |
|              | `get_layer_info`       | 获取图层列表及当前活动层                        |
| **AI 生成**  | `send_to_ai_engine`    | 将图源 + Prompt 发送给 OpenPencil，返回 SVG/PNG |
|              | `render_svg_to_png`    | 将 SVG 按指定尺寸渲染为 PNG                     |
|              | `get_current_svg`      | 从 OpenPencil 右窗获取当前 SVG 源码             |
| **图库管理** | `save_to_gallery`      | 保存图片 + 标签 + 分组到图库                    |
|              | `search_gallery`       | 按标签/文本搜索历史资产                         |
|              | `get_gallery_image`    | 按 ID 获取图库原图                              |

---

## 5. 典型工作流场景

### 场景一：AI 辅助 Logo 设计

1. 用户在中央画布草绘一个简单形状
2. 框选区域 → 右下角输入：“把这个变成科技感 Logo，蓝色调”
3. AI 自动抓取选区 → 调用 OpenPencil 生成 → 预览弹窗展示 3 个方案
4. 用户选择方案 2 → 点击“微调” → 右窗展开 OpenPencil
5. 用户手动调整曲线和文字 → 满意后点击“OK”
6. 结果覆盖到中央画布，同时自动归档图库（带标签：“Logo”“科技感”）

### 场景二：一键生成多尺寸 APP Icon

1. 用户在中央画布完成 Logo 主图形设计
2. 对 AI 助理说：“导出 iOS 全部尺寸，标签‘V2.0 正式版’”
3. AI 调用 `get_canvas_selection` 获取图源
4. 循环调用 `render_svg_to_png`，遍历预设尺寸（20/29/40/60/76/83.5/1024）
5. 每张自动调用 `save_to_gallery`，打上统一标签
6. 全部完成后，AI 回复：“已生成 8 个尺寸，已存入图库”

### 场景三：智能召回历史素材

1. 用户在新项目中输入：“找一下上周做的那组蓝色 Logo”
2. AI 调用 `search_gallery`，按语义搜索图库
3. 返回缩略图列表，用户选择后一键置入当前画布

---

## 6. 开发路线图

### 🥇 阶段一（MVP）—— 核心闭环（第 1-3 周）

- [ ] Tauri v2 + Vue 3 项目初始化
- [ ] 中央画布基础实现（图层 + 画笔 + 选区）
- [ ] WebView 嵌入 OpenPencil
- [ ] 实现“截图 → 传 OpenPencil → 预览 → 落回画布”闭环
- [ ] 实现基础图库（SQLite + 缩略图）
- [ ] 原子工具 1-4 开发（画布交互类）

**验证标准**：用户能完成“选图 → AI 生成 → 落回”全流程，耗时 < 30 秒

### 🥈 阶段二 —— AI 助理上线（第 4-6 周）

- [ ] Rust 后端嵌入 Hermes Agent
- [ ] 右下角 AI 对话浮窗开发
- [ ] 实现 MCP 协议，注册全部 10 个原子工具
- [ ] AI 自主决策能力调优
- [ ] 支持用户配置大模型 API（OpenAI / Claude / DeepSeek / Ollama）
- [ ] 实现 Logo 批量导出场景（通过 AI 编排工具完成）

**验证标准**：用户能用自然语言完成“Logo 生成 → 导出多尺寸”全流程

### 🥉 阶段三 —— 智能增强（第 7-8 周）

- [ ] 引入 LanceDB 向量搜索
- [ ] 图库语义搜索上线（CLIP 模型）
- [ ] AI 自动标签建议
- [ ] 插件系统架构设计（支持第三方工具接入 MCP）
- [ ] 性能优化（大画布渲染、缩略图生成速度）

**验证标准**：用户输入“蓝色背景的手机图标”能召回历史素材

### 🏁 阶段四 —— 发布与社区（第 9-10 周）

- [ ] 跨平台打包（Windows .exe / macOS .dmg / Linux AppImage）
- [ ] 用户文档与教程编写
- [ ] GitHub 开源，建立 Issue / PR 模板
- [ ] 发布 v1.0.0 Alpha 版本，邀请内测

---

## 7. 大模型接入方案（参考 DeepSeek Harness）

OpenPaint **不绑定任何大模型厂商**，用户自行配置：

```yaml
# ~/.openpaint/config.yaml
llm:
  provider: 'openai' # 或 "anthropic" | "deepseek" | "ollama"
  api_key: 'sk-xxx'
  base_url: 'https://api.openai.com/v1' # 可自定义代理
  model: 'gpt-4o' # 或 "claude-3-5-sonnet" | "deepseek-chat"
  local_model: 'qwen2.5:7b' # 如使用 Ollama
```

**隐私承诺**：所有 AI 调用均为用户自主配置，OpenPaint 不收集任何数据。

---

## 8. 开源与社区

### 8.1 开源协议

**MIT License** —— 允许任意使用、修改、分发，包括商业用途。

### 8.2 贡献指南

- 欢迎提交 Issue 和 PR
- 核心模块（画布 / AI 桥接 / 图库）各自独立，方便分工
- 插件 API 稳定后，鼓励社区开发第三方扩展

### 8.3 社区渠道

- GitHub Issues：技术反馈
- Discord 频道：用户交流与创意碰撞
- 示例仓库：官方提供 Prompt 模板和场景配置

---

## 9. 成功指标

| 指标                          | 目标值                |
| :---------------------------- | :-------------------- |
| MVP 发布后月活                | > 500 名设计师/开发者 |
| 核心闭环操作耗时              | < 30 秒               |
| AI 场景完成率（无需人工干预） | > 80%                 |
| GitHub Star（半年）           | > 2k                  |
| 社区贡献者（半年）            | > 20 人               |

---

## 10. 附录

### 附录 A：目录结构（建议）

```
openpaint/
├── src-tauri/           # Rust 后端
│   ├── src/
│   │   ├── main.rs
│   │   ├── canvas/      # 画布引擎
│   │   ├── agent/       # Hermes Agent 集成
│   │   ├── gallery/     # 图库管理 (SQLite)
│   │   └── tools/       # 原子工具实现
│   └── Cargo.toml
├── src-web/             # Vue 3 前端
│   ├── components/
│   │   ├── Canvas.vue
│   │   ├── AIAssistant.vue
│   │   └── OpenPencilView.vue
│   └── main.ts
├── config/              # 预设尺寸 / Prompt 模板
├── docs/                # 用户文档
└── README.md
```

### 附录 B：预设尺寸模板（JSON）

```json
{
  "web": [16, 32, 48, 180, 192, 512],
  "ios": [20, 29, 40, 60, 76, 83.5, 1024],
  "android": [48, 72, 96, 144, 192, 512],
  "favicon": [16, 32, 64]
}
```

---

> **“OpenPaint —— 你的画布，你的 AI，你的规则。”**

---
